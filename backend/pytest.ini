[pytest]
markers =
    integration: marks tests as integration tests (require running services like RabbitMQ, Redis, etc.)
    unit: marks tests as unit tests (no external dependencies)
    performance: marks tests as performance tests (require running services and measure throughput/latency)
